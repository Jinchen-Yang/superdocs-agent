@jsr:registry=https://npm.jsr.io
# onnxruntime-node 运行库默认从境外 NuGet(api.nuget.org / aiinfra.pkgs.visualstudio.com)下载,
# 国内服务器 TLS 常失败 → mastra build 失败。npm 包已捆绑 CPU 推理所需 .so/.node(fastembed 走 CPU),
# 跳过这个境外下载即可,构建自包含、无需联外网。CUDA/TensorRT provider 用不到。
onnxruntime_node_install=skip
